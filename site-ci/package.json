{
  "name": "velvet-scroll-site-deployment",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.14.0" },
  "scripts": {
    "test": "node --test *.test.mjs && python3 -m unittest discover -s . -p '*_test.py'"
  },
  "devDependencies": { "wrangler": "4.130.0" }
}
